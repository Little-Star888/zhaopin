# 工作包 M13-N1-WP4：采集控制冒烟检测

> 目标：端到端验证采集控制链路修复
> 角色：测试
> 预估改动量：无代码改动

## 1. 前置条件
- M13-N1-WP1 ~ WP3 全部通过

## 2. 测试场景
1. **开关验证**：`ENABLE_AUTO_BOOTSTRAP: false` 时刷新扩展，确认不自动采集
2. **手动触发**：Dashboard 点击"开始采集"，确认可正常启动
3. **停止中断**：采集进行中点击"停止"，确认在下一个检查点中断
4. **停止后数据**：停止后确认已采集数据正常入库
5. **模式区分**：分别通过 Dashboard 和自动调度触发采集，确认关键词和冷却策略按模式区分
6. **回归验证**：`ENABLE_AUTO_BOOTSTRAP: true` 时刷新扩展，确认自动调度不受影响

## 3. 验证清单
- [ ] 所有场景通过
- [ ] 不影响 M12 已完成的自动调度功能
