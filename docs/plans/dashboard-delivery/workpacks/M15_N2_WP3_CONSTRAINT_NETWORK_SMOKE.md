# 工作包 M15-N2-WP3：约束与网络冒烟检测

> 目标：端到端验证数据约束与网络优化
> 角色：测试
> 预估改动量：无代码改动

## 1. 前置条件
- M15-N2-WP1 ~ WP2 全部通过

## 2. 测试场景
1. **UNIQUE 约束**：尝试插入重复 provider 到 `ai_configs`，确认被忽略
2. **约束兼容**：检查已有数据无重复 provider
3. **fetch 正常**：`executeInPageContext` 正常请求完成无异常
4. **fetch 超时**：模拟超时场景，Network 面板确认请求 canceled
5. **回归验证**：M13 的采集、首页、工作台功能不受影响

## 3. 验证清单
- [ ] 所有场景通过
- [ ] M15 全部完成判定项已满足
