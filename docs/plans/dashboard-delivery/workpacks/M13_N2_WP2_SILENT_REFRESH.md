# 工作包 M13-N2-WP2：静默刷新确认

> 目标：确认首页静默刷新机制在批次视图下仍正常工作
> 角色：前端
> 预估改动量：验收为主，可能微调

## 1. 前置条件
- M13-N2-WP1 通过（批次视图已实现）

## 2. 读文件
| 文件 | 读取目的 |
|------|---------|
| `crawler/extension/dashboard.js` | 轮询刷新逻辑、DOM diff 或重渲染条件 |

## 3. 改动规格
- 验收现有静默刷新实现：
  - 5 秒轮询获取数据
  - 数据不变时不触发 DOM 重渲染
  - 数据变化时增量更新（非整页重绘）
- 如果批次视图接入后发现闪动问题，按需微调
- **不改轮询间隔**（维持 5 秒）

## 4. 验证
- [ ] 5 秒轮询时首页不闪动
- [ ] 数据不变时不触发 DOM 重渲染
- [ ] 新数据入库后首页自动更新显示
- [ ] 批次视图和全库视图切换后静默刷新均正常
