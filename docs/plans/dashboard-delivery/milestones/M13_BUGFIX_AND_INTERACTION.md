# 里程碑 M13：Bug 修复与交互重构

> 状态：待开始 *(生命周期：待开始 → 进行中 → 阻塞 → [done])*
> 前置：[M12 多平台爬虫](./M12_MULTI_PLATFORM_CRAWLERS.md)
> 排障记录：[M11/M12 手动采集排障记录](../workpacks/M11_M12_MANUAL_CRAWL_DEBUG_REPORT_20260327.md)

## 核心依赖

- M12 全部通过（或当前 Dashboard 手动采集链路可用）
- 排障记录中 2.1~2.10 的 10 个问题已确认并达成解决方案

## 宏观交付边界

- **不动 M10/M11/M12 的已完成成果**
- **仅修复 Bug 和调整交互**，不引入新功能、新平台、新架构
- **分 4 节点串行推进**：控制链路 → 数据展示 → Boss 数据 → UI 交互
- **数据驱动验证**：URL 构造和统计口径需实际验证后再确认修复方案

## 节点路径与状态

| 节点 | 状态 | 角色 | 依赖 | 跳转 |
|------|------|------|------|------|
| [M13-N1：采集控制修复](../nodes/M13_N1_CRAWL_CONTROL_FIX.md) | 待开始 | 后端 | M12 全部 | → WP1~WP4 |
| [M13-N2：首页数据展示修复](../nodes/M13_N2_HOME_DATA_VIEW_FIX.md) | 待开始 | 前端、后端 | M13-N1 | → WP1~WP3 |
| [M13-N3：Boss 数据修复](../nodes/M13_N3_BOSS_DATA_FIX.md) | 待开始 | 后端 | M13-N1 | → WP1~WP3 |
| [M13-N4：工作台交互修复](../nodes/M13_N4_WORKBENCH_INTERACTION_FIX.md) | 待开始 | 前端 | M13-N2 | → WP1~WP3 |

## 执行拓扑

```
M12 全部 ──→ M13-N1（采集控制修复，4 WP）──┐
                                                    ├──→ M13-N2（首页数据展示，3 WP）
                                                    └──→ M13-N3（Boss数据修复，3 WP）──→ M13-N4（工作台交互修复，3 WP）
```

> N2 依赖 N1（控制链路稳定后再做数据展示）。N3 与 N2 可部分并行（N3 的 URL 构造和统计修复不依赖 N2 的批次视图），但 N3 的验证结果需要在 N2 首页上展示，因此串行更可控。N4 依赖 N2（收藏状态联动需要在首页验证）。

## 执行优先级

> **必须按以下顺序执行**，控制链路稳定是一切后续联调的基础。

1. **第一步：关闭自动 bootstrap（N1-WP1）** — 避免联调时被自动采集干扰
2. **第二步：修复真正可停止机制（N1-WP2）** — 确保系统具备可靠的中断能力
3. **第三步：模式区分与冷却收敛（N1-WP3）** — 手动/自动模式明确分离
4. **第四步：数据展示与交互改造（N2 → N3 → N4）** — 在基建稳固后执行

## 完成判定

### 采集控制
- [ ] 扩展刷新后不自动开始采集（`ENABLE_AUTO_BOOTSTRAP` 开关生效）
- [ ] 点击"停止采集"后，当前采集任务可在下一个检查点中断
- [ ] 手动模式与自动模式在冷却、过滤策略上明确区分
- [ ] 手动模式冷却从"完全绕过"收敛为"缩短窗口（3-5 分钟）

### 首页数据展示
- [ ] 首页默认展示本次采集结果（按 `crawl_batch_id` 过滤）
- [ ] 保留"全部岗位"入口，用户可手动切回
- [ ] 首页静默刷新稳定，数据不变时不重渲染

### Boss 数据
- [ ] Boss 岗位显示可访问的原链接（encryptJobId + securityId 构造）
- [ ] "含描述条数"统计与实际详情一致

### 工作台交互
- [ ] 50/50 分屏只在工作台页面内展示，首页无串扰
- [ ] 分屏打开时页面不撑长，无需滚动
- [ ] "待投递"文案全部替换为"收藏"模型
- [ ] 收藏/取消收藏后首页、工作台、分屏三处状态联动一致
